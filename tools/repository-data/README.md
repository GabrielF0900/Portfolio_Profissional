# Dados auxiliares de repositórios

`fetch_repos.ps1` consulta a API pública do GitHub para atualizar dados dos repositórios. `repositorios_consolidado.json` é o snapshot consolidado mantido para consulta editorial; ele não participa do build da aplicação.

Execute o coletor a partir desta pasta para manter suas saídas agrupadas aqui.
