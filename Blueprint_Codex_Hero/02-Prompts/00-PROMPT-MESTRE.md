# PROMPT MESTRE — EXECUTOR DO BLUEPRINT

Continue na branch `redesign-v2`.

A partir daqui você NÃO deve redesenhar livremente o Hero.
O design já foi decidido. Sua função é executar este blueprint.

Antes de alterar:
1. `git status`
2. `git branch --show-current`
3. `git log -1 --oneline`
4. confirme `redesign-v2`
5. continue aplicando `gpt-taste`, subordinada ao blueprint
6. leia todos os arquivos em `Blueprint_Codex_Hero/01-Especificacoes/`
7. analise Hero alvo e estado atual
8. use Motion apenas como intenção

Código atual = fonte da verdade dos dados.
Nunca copiar dado fictício da imagem.

Nunca executar duas microfases juntas.
Nunca avançar para Sobre/Experiência.
Depois de cada microfase: validar, relatar e PARAR.
